[ftp]
host = inta-ddp.inta.es
user = inta-cadip
password = X214_Opmas

[paths]
# Base directory for CADIP scripts and data
cadip_base = /home/meos/bin/cadip

# Working directories
in_dir = %(cadip_base)s/IN
md5_dir = %(cadip_base)s/MD5
sent_files_dir = %(cadip_base)s/Sent_DDP
bad_from_ddp_dir = %(cadip_base)s/BAD_From_DDP
ftp_results_dir = %(cadip_base)s/ftp-results
lock_file = %(cadip_base)s/cadip_upload.lock

[report_generator]
# Source directory where acquisition folders (SXY_xxxxxxxx) are created
reports_source = /disk3/reports
# Pass index: folder state keyed by satellite+orbit (rows dropped when the folder is gone)
processed_file = /home/meos/bin/cadip/processed_folders.txt
# Station identifier for the EOF filename
station_id = MPS

[remote]
# Remote directory template — {unit} is replaced by the DFEP processing unit (1-9)
target_path_template = WebReports/DFEP{unit}/
